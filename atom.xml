<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>http://www.guotingyan.cn</id>
    <title>醉墨</title>
    <updated>2020-11-19T14:48:17.066Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="http://www.guotingyan.cn"/>
    <link rel="self" href="http://www.guotingyan.cn/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>http://www.guotingyan.cn/images/avatar.png</logo>
    <icon>http://www.guotingyan.cn/favicon.ico</icon>
    <rights>All rights reserved 2020, 醉墨</rights>
    <entry>
        <title type="html"><![CDATA[git 的使用方法]]></title>
        <id>http://www.guotingyan.cn/post/git-remote-de-shi-yong-fang-fa/</id>
        <link href="http://www.guotingyan.cn/post/git-remote-de-shi-yong-fang-fa/">
        </link>
        <updated>2020-04-01T06:16:27.000Z</updated>
        <content type="html"><![CDATA[<h3 id="git-remote-的使用方法">git remote 的使用方法</h3>
<ol>
<li>创建好项目之后使用 git init ;</li>
<li>git remote -v 查看仓库的远程</li>
<li>使用 git remote add origin 远程仓库链接</li>
<li>git remote -v 再次查看仓库的远程</li>
<li>git add ./ 添加要提交的文件</li>
<li>git commit -m ‘提交名称’</li>
<li>提交前先拉一下远程仓库，git pull origin master；</li>
<li>若有冲突使用git merge origin/master --allow-unrelated-histories合并冲突</li>
<li>git push -u origin master 推送到远程仓库</li>
</ol>
<h3 id="创建分支">创建分支</h3>
<ol>
<li>git checkout -b 分支名</li>
<li>git branch</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Javascript 中的 new 操作符]]></title>
        <id>http://www.guotingyan.cn/post/javascript-zhong-de-new-cao-zuo-fu/</id>
        <link href="http://www.guotingyan.cn/post/javascript-zhong-de-new-cao-zuo-fu/">
        </link>
        <updated>2020-03-30T02:50:18.000Z</updated>
        <content type="html"><![CDATA[<p>在 js 中 new 操作符到底实现什么，在经过网上查询之后我总结了一下，留做笔记。</p>
<p>首先，试一下原生 js 中的 new 的作用</p>
<pre><code class="language-javascript">function Person(name , age) {
    this.name = name,
    this.age = age
}

Person.prototype.sayName = function(){
    console.log(this.name)
}
let per = new Person( '小明', 12 )

console.log(per)
per.sayName()

// Person { name: '小明', age: 12 }
// 小明

</code></pre>
<p>由上面可以看出 js 中的 new 操作符在 new 的过程中：</p>
<ol>
<li>创建一个以构造函数的原型为原型的对象即 Object.create(Person.prototype)</li>
<li>将构造函数的 this 绑定到上一步所创建的对象上</li>
<li>返回创建的对象<br>
用代码实现：</li>
</ol>
<pre><code class="language-javascript">
function myNew(parent , ...arg){
    // 创建一个以构造函数的原型为原型的空对象
    let obj = Object.create(parent.prototype);
    // 绑定构造函数的 this 到新对象上
    parent.apply(obj,arg)
    // 返回新创将的对象
    return obj
}

let per1 = myNew(Person,'小明', 12)

console.log(per)
per.sayName()
// Person { name: '小明', age: 12 }
// 小明


</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[面试总结]]></title>
        <id>http://www.guotingyan.cn/post/mian-shi-zong-jie/</id>
        <link href="http://www.guotingyan.cn/post/mian-shi-zong-jie/">
        </link>
        <updated>2020-02-24T08:19:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="盒模型">盒模型</h2>
<ol>
<li>标准盒模型<br>
盒子的实际宽度 = width + padding + border + margin<br>
width = content</li>
<li>IE 的怪异盒模型<br>
盒子的实际宽度 = width + margin<br>
width = content + padding + border</li>
<li>可以使用 box-sizing属性来切换盒模型</li>
</ol>
<h2 id="布局">布局</h2>
<ol>
<li>定位布局 （position： static 、 relative 、 absolute 、 fixed 、 sticky  ）
<blockquote>
<ol>
<li>默认为 static</li>
<li>relative ： 相对定位 相对于自身之前的位置移动来进行定位</li>
<li>absolute ： 绝对定位 相对于父级部位 static 的元素来进行定位</li>
<li>fixed：固定定位 相对于浏览器窗口来进行定位</li>
<li>sticky：粘性定位 固定定位于绝对定位的结合</li>
</ol>
</blockquote>
</li>
<li>流布局，就是使用百分比布局，通常配合 max-/min-来进行约束， 根据浏览器窗口的分辨率来进行调节显示元素的大小，<br>
使得整体布局不变 。<br>
缺点： 在屏幕过大或过小情况下导致元素显示不正常</li>
<li>浮动布局 ：使用 float 属性，使元素脱离文档流浮动，但不会脱离文本流，因此可以<br>
实现文字环绕的效果，会导致很多问题<br>
常用清除浮动方法：
<blockquote>
<ol>
<li>额外标签法：在父元素末尾添加一个空标签加上 clear：both；</li>
<li>触发BFC overflow：hidden；会导致溢出部分被隐藏</li>
<li>使用 css3 的伪元素来清除浮动</li>
</ol>
</blockquote>
</li>
<li>圣杯布局 ： 利用负边距于相对定位来进行布局，形成左右两边固定中间位置自适应的三栏布局</li>
<li>flex 布局</li>
</ol>
<pre><code class="language-css">     1. 父元素设置flex 属性后，子元素的float、clear、vertical-align 将失效
     2. flex-direction:  决定主轴方向
        1. row 从左往右 ，
        2. row-reverse 从右往左
        3. column 从上往下
        4. column-reverse 从下往上
     3. flex-wrap: 元素换行方式
        1. nowrap （默认）不换行
        2. wrap 换行，第一行在上方
        3. wrap-reverse 换行，第一行在下方
     4. justify-content 在主轴上的对齐方式
        1. flex-start（默认值）：左对齐
        2. flex-end：右对齐
        3. center： 居中
        4. space-between：两端对齐，元素之间的间隔都相等。
        5. space-around：每个元素两侧的间隔相等。所以，元素之间的间隔比元素与边框的间隔大一倍。
     5. align-items 交叉轴上的对齐方式
        1. flex-start：交叉轴的起点对齐。
        2. flex-end：交叉轴的终点对齐。
        3. center：交叉轴的中点对齐。
        4. baseline: 元素的第一行文字的基线对齐。
        5. stretch（默认值）：如果元素未设置高度或设为auto，将占满整个容器的高度。 
     6. align-content 多根轴线的对齐方式，若只有一根轴线则不起作用
     7. flex-grow 元素的放大比例 
     8. flex : 是flex-grow（放大比例）, flex-shrink（缩小比例） 和 flex-basis（基础占用空间）
     9. align-self: 可以用来覆盖 align-items 的属性，来设置单个元素的对齐方式
</code></pre>
<ol start="6">
<li>grid 布局</li>
</ol>
<pre><code class="language-css">     1. 父元素设置grid 属性后，子元素的float、display: inline-block、display: table-cell、vertical-align和column-* 将失效
     2. grid-template-columns:指定每列列宽，grid-template-rows：指定每行行高
     3. auto-fill 自动填充，直到装不下下一个单元格
     4. fr( fraction:片段 ) 是单元格比例关系
     5. grid-gap 设置行列间隔，为 grid-row-gap 与grid-column-gap 的缩写
    待续。。。
</code></pre>
<ol start="7">
<li>
<p>BFC 块格式化上下文 (Block Formatting Context)</p>
<blockquote>
<ol>
<li>创建：<br>
1. 浮动元素 (元素的 float 不是 none)<br>
2. 绝对定位元素 (元素具有 position 为 absolute 或 fixed)<br>
3. 内联块 (元素具有 display: inline-block)<br>
4. 表格单元格 (元素具有 display: table-cell，HTML表格单元格默认属性)<br>
5. 具有overflow 且值不是 visible 的块元素，</li>
<li>作用：<br>
1. 清除浮动<br>
2. 产生边界(与浮动元素)</li>
</ol>
</blockquote>
</li>
<li>
<p>数据存储 （ cookie、sessionStorage、localStorage ）</p>
<blockquote>
<ol>
<li>cookie : 会话 cookie 不设置过期时间，关闭浏览器窗口失效<br>
设置过期时间，超过时间，cookie 失效<br>
特点： 每次请求浏览器会将 cookie 通过 http 的请求头发送给服务端，安全性能低，浪费带宽，且 cookie只能存储 4kb 数据，<br>
还只能是文本字符串</li>
</ol>
</blockquote>
<blockquote>
<ol start="2">
<li>sessionStorage : 浏览器本地存储，大小 5M ，当浏览器窗口关闭则失效，也是会话级存储，</li>
<li>localStorage: 浏览器本地存储，大小 5M，浏览器窗口关闭不会消失，除非手动清除，否则永久存在。</li>
<li>相对于 cookie 而言 webStorage的安全性更高且不会发送给服务器，并且浏览器有提供操作的 api，更加便于操作。</li>
</ol>
</blockquote>
</li>
<li>
<p>animation和transiton的相关属性</p>
<blockquote>
<ol>
<li>animation ( 动画名称，动画播放时间，动画速度曲线，动画延迟时间，动画播放次数，动画是否反向播放 )</li>
<li>transiton ( 过度属性名称，过度时间，过度曲线 ，过度延迟时间 )</li>
</ol>
</blockquote>
</li>
</ol>
<h1 id="javascript">JavaScript</h1>
<h2 id="数据类型">数据类型</h2>
<ol>
<li>基础数据类型</li>
</ol>
<pre><code class="language-javascript">        1. number
            - typeof NaN  // number
        2. string
        3. boolean
        4. symbol
            - Symbol 实例是唯一存在的的
            - 主要用来作为对象键名的标识符
            - 以 Symbol 作为标识符的键名，不可以被枚举，可以通过 getOwnPropertySymbols 来查询到
        5. undefined
            - typeof undefined  // undefined
        6. null
            - typeof null // object
</code></pre>
<ol start="2">
<li>引用数据类型</li>
</ol>
<pre><code class="language-javascript">        1. Object
        2. Array
        3. Function
        ...
</code></pre>
<h2 id="作用域">作用域</h2>
<ol>
<li>作用域链：<br>
1. 在 JavaScript 中函数会形成一个单独的作用域，整个作用域是由全局作用域以及每一个函数形成的作用域所组成，在外部的作用域不能访问内部作用域<br>
中的变量，而在内部作用域可以访问其外部作用域中的变量，在 js 中，若调用一个变量，会首先在当前作用域中查找，如果没找到就会去上一级作用域中查找<br>
一直到查找到顶级也就是全局作用域，若没找到，则会提示' 变量名 is not defined' ，在这每一级的查找过程中，作用域形成了链式结构，便叫做作用域<br>
链<br>
2. 闭包：闭包是由函数以及创建该函数的词法环境所组成的，这个词法环境就是这个函数创建时所能访问到的所有的局部变量。<br>
1. 闭包作用：<br>
1. 常用于变量的私有化。<br>
2. 保存外部变量，被闭包所引用的外部变量不会被 js 引擎回收<br>
2. 缺点：<br>
1. 大量使用闭包会造成内存泄漏，影响性能，造成网页卡顿。 ( 解决方法： 手动删除被闭包引用的局部变量 )</li>
</ol>
<h2 id="javascript继承">JavaScript继承</h2>
<ol>
<li>构造函数继承
<blockquote>
<p>使用 call、apply 来实现</p>
</blockquote>
</li>
</ol>
<pre><code class="language-javascript">            function Person(name){
                 this.name = name;
                 this.species = 'people'
            }
            function Man(name ,sex){
                Person.apply(this,arguments);
                this.sex = sex
            }
            const a = new Man('a','男')
            
            console.log(a)  // Man { name: 'a', species: 'people', sex: '男' }
</code></pre>
<p>缺点：<br>
1. 每个实例都拷贝一份父类的方法，占用内存大，尤其是方法过多的时候，因此可以用原型继承来解决占用内存过大的窘境）<br>
2. 当需求变更要修改方法时，之前所生成的实例方法是无法及时更新的。</p>
<ol start="2">
<li>原型继承
<ol>
<li>原型链：</li>
</ol>
</li>
</ol>
<pre><code class="language-javascript">                function Person(name){
                       this.name = name;
                }
                Person.prototype.color = 'yellow';
                
                // 构造函数 Person 的prototype 属性指向 一个对象，这个对象就是 Person 的原型，
                const mash = new Person('mash');
                console.log(mash.name) // mash
                console.log(mash.color) // yellow
                
                //  由构造函数 Person 生成的实例 mash 拥有 color 属性， 这个属性继承自 Person.property
                //  即实例会继承原型上的属性。 与此同时，实例 mash 的__proto__属性指向构造函数原型，可以访问到原型上的属性
                //  由于构造函数的原型 Person.prototype 是一个对象，它也会拥有创造它的构造函数，也会有原型，因此它也可以访问它的原型上的属性，
                //  由此一层一层往上形成一条原型链，原型链的顶端为 null（null 在js 中也是一个对象）。
                
                //  在js 中查找对象的属性时有一条这样的查找规则:当对象本身没有这个属性的时候，js 引擎会查找该对象的原型，并顺着原型链一级一级的找上去
                //  直到找到 null 为止，如果没找到，则会显示为 undefined；
</code></pre>
<ol>
<li>原型继承：即将子类的原型链接到父类的实例上即可实现原型继承</li>
</ol>
<pre><code class="language-javascript">                  function Person(name){
                          this.name = name;
                  }
                  Person.prototype.sayHello = function(){ console.log('Hello '+ this.name) } ;
                  
                  function Student(level){
                        this.level = level;
                  }
                  
                  Student.prototype = new Person( '小明' )
                  Student.prototype.constructor = Student;
                  const x = new Student(1)
                  
                  console.log(x.name) // 小明
</code></pre>
<p>缺点： 在上方的实现中重写了 Student 的原型指向，这就会导致重写之前所创建的实例仍然指向之前的原型.</p>
<ol start="3">
<li>组合继承
<blockquote>
<p>为了解决以上两种继承所展现出的问题，可以使用组合的方式来避免他们各自的缺陷。</p>
</blockquote>
</li>
</ol>
<pre><code class="language-javascript">             function Person(name){
                  this.name = name;
             }
             Person.prototype.sayHello = function(){ console.log('Hello '+ this.name) } ;
                              
             function Student(level){
                    Person.apply(this,arguments)  // 第二次调用构造函数
                    this.level = level;
             }
                              
             Student.prototype = new Person( '小明' )    // 第一次调用构造函数
             Student.prototype.constructor = Student;
             const x = new Student(1) 
                              
             console.log(x.name) // 小明
</code></pre>
<p>缺点：无论在什么情况下，都会调用两次父类构造函数：一次是在创建子类型原型时，另一次是在子类型构造函数内部。</p>
<ol start="4">
<li>寄生继承 : 寄生继承就是不用实例化父类了，直接实例化一个临时副本实现了相同的原型链继承。（即子类的原型指向父类副本的实例从而实现原型共享）</li>
</ol>
<pre><code class="language-javascript">            function Training(child,person){
                const newPerson = function(){};
                newPerson.prototype = person.prototype
                child.prototype = new newPerson()
                child.prototype.constructor = child;
            }
            function Person(name){
                this.name = name;
            }
            Person.prototype.sing = function () {
                console.log('sing song')
            }
             function Student(age){
                  this.age = age;
             }
            Training(Person)
            const b = new Student(1)
            
            b.sing()
</code></pre>
<ol start="5">
<li>寄生组合继承：
<ol>
<li>使用构造函数方式继承属性</li>
<li>使用原型继承方式继承方法</li>
<li>使用寄生继承方式避免调用两次父类构造函数</li>
</ol>
</li>
</ol>
<pre><code class="language-javascript">              function extend(child,parent){
                             const newPerson = function(){};
                             newPerson.prototype = person.prototype
                             child.prototype = new newPerson()
                             child.prototype.constructor = child;
                         }
                         function Person(name){
                             this.name = name;
                         }
                         Person.prototype.sing = function () {
                             console.log('sing song')
                         }
                        function Student(name,level){
                            Person.call(this,arguments);
                            this.level=level
                        }
                        extend(Student,Person)
</code></pre>
<h2 id="前端跨域解决jsonpcors">前端跨域解决：（JSONP，CORS）</h2>
<p>HTTP 响应中包含一个状态码，用来表示服务器对客户端响应的结果。<br>
状态码一般由3位构成：</p>
<p>1xx : 表示请求已经接受了，继续处理。<br>
2xx : 表示请求已经处理掉了。<br>
3xx : 重定向。<br>
4xx : 一般表示客户端有错误，请求无法实现。<br>
5xx : 一般为服务器端的错误。</p>
<p>比如常见的状态码：</p>
<p>200 OK 客户端请求成功。<br>
301 Moved Permanently 请求永久重定向。<br>
302 Moved Temporarily 请求临时重定向。<br>
304 Not Modified 文件未修改，可以直接使用缓存的文件。<br>
400 Bad Request 由于客户端请求有语法错误，不能被服务器所理解。<br>
401 Unauthorized 请求未经授权，无法访问。<br>
403 Forbidden 服务器收到请求，但是拒绝提供服务。服务器通常会在响应正文中给出不提供服务的原因。<br>
404 Not Found 请求的资源不存在，比如输入了错误的URL。<br>
500 Internal Server Error 服务器发生不可预期的错误，导致无法完成客户端的请求。<br>
503 Service Unavailable 服务器当前不能够处理客户端的请求，在一段时间之后，服务器可能会恢复正常。</p>
<p>作者：只会番茄炒蛋<br>
链接：https://juejin.im/post/5e3d898cf265da5732551a56<br>
来源：掘金<br>
著作权归作者所有。商业转载请联系作者获得授权，非商业转载请注明出处。</p>
<ol>
<li>跨域的原因：<br>
由于浏览器的同源策略即当（端口、协议、域名）相同时两个脚本才可以进行交互</li>
<li>JSONP 实现跨域<br>
JSONP原理：</li>
</ol>
<blockquote>
<p>利用script标签不受同源策略的限制，向后端请求 js 文件，后端返回的 js 文件内包含一个函数的调用，并且会传回前端所需要的数据作为参数，只需现在前端页面预先写好需要后端执行的函数体即可</p>
</blockquote>
<ol start="3">
<li>
<p>CORS（跨域资源共享）</p>
<blockquote>
<p>简单请求：<br>
请求方法（ HEAD，GET，POST ）<br>
http 头信息字段  （ Accept，Accept-Language，Content-Language， Last-Event-ID，Content-Type （不为 application/json）  ）<br>
同时符合以上两种情况都为简单请求</p>
</blockquote>
</li>
<li>
<p>CORS 是在 http 请求头上添加额外的信息，使得 web 可以被允许访问不同源的资源+</p>
</li>
<li>
<p>头部信息：</p>
<ul>
<li>request：</li>
</ul>
<ol>
<li>对于简单请求：<br>
浏览器会在 http 请求头上加一个 origin 的字段传递当前的源信息（域名、端口、协议），若源被后端服务器允许则响应头 会被加上<br>
Access-Control-Allow-Origin 的字段，若不被服务器允许，则无此字段，会被XMLHttpRequest的onerror回调函数捕获</li>
</ol>
<ul>
<li>response：</li>
</ul>
<ol>
<li>Access-Control-Allow-Origin： 用来指定那些域名的客户端可以访问本服务器上的资源，当请求头携带有 cookie 时不可以使用*，</li>
<li>Access-Control-Allow-Credentials ： 若服务器支持通过 cookie传递验证信息，则该选项变回存在的返回数据里，只有一个值就是true，于此同时在前端也要打开withCredentials字段,即：</li>
</ol>
</li>
</ol>
<pre><code class="language-javascript">   var xhr = new XMLHttpRequest();
   xhr.withCredentials = true;
</code></pre>
<p>否则即使服务器同意发送 cookie，浏览器也不会发送</p>
<ol>
<li>Access-Control-Allow-Headers 服务器支持的请求数据类型</li>
<li>Access-Control-Allow-Methods 服务器支持的请求类型<br>
对于非简单请求，浏览器会发送一个预检请求，当预检请求通过后服务端会返回一个Access-Control-Max-Age字段，表示预检请求的过期时间，在此时间内，非简单请求与简单请求一样</li>
</ol>
<p>相比于 JSONP，CORS功能更强大，支持方法更多，JSONP 仅仅支持 GET 方法，JSONP 优势在与某些不支持 CORS 的老版本浏览器</p>
<h2 id="节流-防抖">节流、防抖</h2>
<ol>
<li>节流 （throttle）： 事件在一定时间内仅仅执行一次</li>
</ol>
<pre><code class="language-javascript">            function throttle(fn ,wait){
                let timer = true
                return function () {
                    if(!timer){
                        return
                    }
                    timer = false;
                    setTimeout(()=&gt;{
                        fn.apply(this,arguments)
                        timer = true;
                    },wait)
                }
            }
</code></pre>
<ol start="2">
<li>防抖（debounce）： 触发事件后一定时间内仅执行一次，若多次触发，重新计算时间</li>
</ol>
<pre><code class="language-javascript">            function debounce(fn,wait) {
                let timer = null;
                return function () {
                    clearTimeout(timer)
                    timer = setTimeout(()=&gt;{
                        fn.apply(this,arguments)
                    },wait)
                }
            }
</code></pre>
<h3 id="vue">Vue</h3>
<ol>
<li>vuex是一个专为vue.js应用程序开发的状态管理器，它采用集中式存储管理应用的所有组件的状态，并且以相<br>
应的规则保证状态以一种可以预测的方式发生变化。</li>
</ol>
<p>state: vuex使用单一状态树，用一个对象就包含来全部的应用层级状态</p>
<p>mutation: 更改vuex中state的状态的唯一方法就是提交mutation</p>
<p>action: action提交的是mutation，而不是直接变更状态，action可以包含任意异步操作</p>
<p>getter: 相当于vue中的computed计算属性</p>
<ol start="2">
<li>Vue是采用数据劫持配合发布者-订阅者模式，通过Object.defineProperty来()来劫持各个属性的getter和setter<br>
在数据发生变化的时候，发布消息给依赖收集器，去通知观察者，做出对应的回调函数去更新视图。</li>
</ol>
<p>具体就是：<br>
MVVM作为绑定的入口，整合Observe,Compil和Watcher三者，通过Observe来监听model的变化<br>
通过Compil来解析编译模版指令，最终利用Watcher搭起Observe和Compil之前的通信桥梁<br>
从而达到数据变化 =&gt; 更新视图，视图交互变化(input) =&gt; 数据model变更的双向绑定效果。</p>
<h3 id="css3">css3</h3>
<p>1.过渡 transition<br>
2.动画 animation<br>
3.形状转换 transform<br>
4.阴影 box-shadow<br>
5.滤镜 Filter<br>
6.颜色 rgba<br>
7.栅格布局 gird<br>
8.弹性布局 flex</p>
<h3 id="computed-与-watch区别">computed 与 watch区别</h3>
<p>①从属性名上，computed是计算属性，也就是依赖其它的属性计算所得出最后的值。watch是去监听一个值的变化，然后执行相对应的函数。<br>
②从实现上，computed的值在getter执行后是会缓存的，只有在它依赖的属性值改变之后，下一次获取computed的值时才会重新调用对应的getter来计算。watch在每次监听的值变化时，都会执行回调。其实从这一点来看，都是在依赖的值变化之后，去执行回调。很多功能本来就很多属性都可以用，只不过有更适合的。如果一个值依赖多个属性（多对一），用computed肯定是更加方便的。如果一个值变化后会引起一系列操作，或者一个值变化会引起一系列值的变化（一对多），用watch更加方便一些。<br>
③watch的回调里面会传入监听属性的新旧值，通过这两个值可以做一些特定的操作。computed通常就是简单的计算。<br>
④watch和computed并没有哪个更底层，watch内部调用的是vm.$watch，它们的共同之处就是每个定义的属性都单独建立了一个Watcher对象。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Javascript基本数据类型之Symbol]]></title>
        <id>http://www.guotingyan.cn/post/javascript-ji-ben-shu-ju-lei-xing-zhi-symbol/</id>
        <link href="http://www.guotingyan.cn/post/javascript-ji-ben-shu-ju-lei-xing-zhi-symbol/">
        </link>
        <updated>2020-01-06T02:37:12.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>Symbol类型是ES6新添加的一种基本数据类型，具有自己的静态属性与静态方法。并且其静态方法会暴露出全局的Symbol注册表。Symbol是一个不完整的构造函数，因为他不能通过new Symbol()来创建Symbol的实例。Symbol创建的值是唯一的，所以它仅有的目的就是为对象作为私有属性的标识符。</p>
</blockquote>
<ol>
<li>Symbol创建实例,且每个Symbol的实例都是独一无二的。</li>
</ol>
<pre><code class="language-javascript">var symbol = Symbol();
var symbol1 = Symbol(&quot;foo&quot;);
console.log(Symbol(&quot;aaa&quot;) === Symbol(&quot;aaa&quot;))
// Symbol()
// Symbol(foo)
// false

</code></pre>
<ol start="2">
<li>Symbol.for(token) 与 Symbol.keyFor(Symbol);<br>
Symbol.for(token) 方法回去Symbol全局注册表中查找是否存在有传入的token字符串所生成的Symbol实例，若有则会返回找到的Symbol实例；若没有则会创建一个新的Symbol实例注册到全局的Symbol注册表中并返回新生成的Symbol实例。</li>
</ol>
<pre><code class="language-javascript">var a = Symbol.for(&quot;a&quot;);
var e = Symbol.for(&quot;a&quot;);
console.log(a === e)
// true
</code></pre>
<p>Symbol.keyFor(Symbol)方法，在全局注册表中寻找传入的Symbol实例是否存在，若存在则返回生成该实例的token字符串，若不存在，则返回undefined。</p>
<pre><code class="language-javascript">var a = Symbol.for(&quot;a&quot;);
console.log(Symbol.keyFor(a) === &quot;a&quot;)
// true
</code></pre>
<ol start="3">
<li>Symbol作为对象的属性的标识符时，是不可以被枚举的，对象的getOwnPropertyNames方法也不能取到该属性，可以通过getOwnPropertySymbols方法来取到对象的全部以Symbol实例作为标识符的属性。</li>
</ol>
<pre><code class="language-javascript">var age = Symbol.for(&quot;age&quot;);
var obj = {};
obj[age] = 20;
obj[&quot;name&quot;] = &quot;小明&quot;;
obj[&quot;sex&quot;] = &quot;男&quot;;
for(var k in obj){
    console.log(obj[k]);
}
console.log(Object.getOwnPropertyNames(obj));
console.log(Object.getOwnPropertySymbols(obj));
// 小明
// 男
// [ 'name', 'sex' ]
// [ Symbol(age) ]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[call、apply与bind的区别]]></title>
        <id>http://www.guotingyan.cn/post/callapply-yu-bind-de-qu-bie/</id>
        <link href="http://www.guotingyan.cn/post/callapply-yu-bind-de-qu-bie/">
        </link>
        <updated>2020-01-05T00:32:09.000Z</updated>
        <content type="html"><![CDATA[<blockquote>
<p>bind 强制改变函数内部this指向，并返回一个函数，故其并不会立即执行<br>
apply 强制改变函数内部运行时this指向，立即执行，其第二个参数为一个数组<br>
call 强制改变函数内部运行时this指向，立即执行，其后从第二个参数开始，每一个参数将传入函数中作为参数执行。</p>
</blockquote>
<ol>
<li>call 函数实现</li>
</ol>
<p>首先，call函数有两个基础功能，一，会将函数的this指向为目标对象，如果没有目标对象就会指向window（浏览器中）全局对象，第二个就是call会立即执行。因此可以将其看做事将这个目标对象添加一个属性指向当前函数然后立即执行，最后将其添加的属性删除。</p>
<pre><code class="language-javascript">    var obj = {
    name:1
    }
    function getname(sex,age) {
    console.log(this.name)
    }
    getname.call(obj) //  1
    // this指向改变了
    // getname函数被执行了
</code></pre>
<p>接着，call函数还可以向函数中添加参数，可以通过函数的默认arguments属性来获取函数接收到的属性，代码如下</p>
<pre><code class="language-javascript">    function getname(sex,age) {
    console.log(this.name)
    console.log(sex)
    console.log(age)
    }
    Function.prototype.call3 = function (context) {
    context.fn = this;
    var args = []
    for(var i = 1;i&lt;arguments.length;i++){
    args.push(&quot;arguments[&quot;+i+&quot;]&quot;)
    }
    eval(&quot;context.fn(&quot;+args+&quot;)&quot;) 
    delete context.fn
    }
    getname.call3(obj,&quot;男&quot;,25)  // 1 男  25
</code></pre>
<ol start="2">
<li>对于apply实现方式与call类似，只不过要判断第二个参数是否存在，实现代码如下</li>
</ol>
<pre><code class="language-javascript">    Function.prototype.myapply = function (context) {
    context.fn = this;
    var arr = arguments[1]
    var args = []
    if(!arr){
        context.fn()
    }else{
        for(var i = 0;i&lt;arr.length;i++){
            args.push(&quot;arr[&quot;+i+&quot;]&quot;)
        }
        eval(&quot;context.fn(&quot;+args+&quot;)&quot;) 
    }
    delete context.fn
}

</code></pre>
<ol start="3">
<li>bind的实现，bind的实现是在apply的基础上实现的，先来分析bind有哪些功能
<ol>
<li>首先bind的会返回一个函数，它不会立即执行</li>
<li>其次bind可以传入参数</li>
<li>bind的返回值可以当做一个构造函数，调用new方法可以生成实例<br>
第一步先实现会返回一个函数，执行可以看见改变this后指向的结果</li>
</ol>
</li>
</ol>
<pre><code class="language-javascript">    obj = {
    name:&quot;Mosh&quot;
    }
    function getInfo(params) {
    console.log(this.name)
    }
    Function.prototype.myBind = function (context) {
    const _self = this;
    return function fn(params) {
    _self.apply(context)
    }
    }
    var objFn = getInfo.myBind(obj)
    objFn()  // Mosh

</code></pre>
<p>由于原生js的bind实现方法可以在调用bind的方法的时候传入参数，也可以在调用bind返回函数执行的时候传入参数，因此要讲两次的参数组合起来，代码如下</p>
<pre><code class="language-javascript">    obj = {
    name:&quot;Mosh&quot;
    }
    function getInfo(sex,age) {
    console.log(this.name)
    console.log(sex)
    console.log(age)

    }

    Function.prototype.myBind = function (context) {
    const _self = this;
    const args = [].slice.call(arguments,1);
    return function fn(params) {
    _self.apply(context,args.concat([].slice.call(arguments)))
    }
    }
    var objFn = getInfo.myBind(obj,15)
    objFn(&quot;男&quot;) 
    //  Mosh
    //  15
    //  男
</code></pre>
<p>然后还有一个最重要的就是构造函数模式的实现，在该模式下由bind模式生成的函数通过new 方法构造的实例，会继承原函数上的所有属性，以及传入的全部参数，只是绑定的this指向失效，重新指向了新生成的实例上，实现代码如下：</p>
<pre><code class="language-javascript">    obj = {
    name:&quot;Mosh&quot;
    }
    function getInfo(sex,age) {
    console.log(this.name)
    console.log(sex)
    console.log(age)
    }
    getInfo.prototype.type = 'function'
    Function.prototype.myBind = function (context) {
    const _self = this;
    const args = [].slice.call(arguments,1);
    function Fn(params) {
    _self.apply( this instanceof Fn ? this : context,args.concat([].slice.call(arguments)))
    }
    var NewFn = this.prototype
    Fn.prototype = new newFn()
    return Fn
    }
    var objFn = getInfo.myBind(obj,15)
    var newObj = new objFn(&quot;男&quot;)
    console.log(newObj.type);
    //undefined
    //15
    //男
    //function

</code></pre>
<p>最后，就是一些细节处理。如果调用bind的不是函数，要报错，完整代码如下：</p>
<pre><code class="language-javascript">    Function.prototype.myBind = function (context) {
    if(typeof this !== &quot;function&quot;){
    throw new Error();
    }
    const _self = this;
    const args = [].slice.call(arguments,1);
    function Fn(params) {
    _self.apply( this instanceof Fn ? this : context,args.concat([].slice.call(arguments)))
    }
    var NewFn = this.prototype
    Fn.prototype = new newFn()
    return Fn
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[从零开始学数据结构之树（一）]]></title>
        <id>http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-zhi-shu/</id>
        <link href="http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-zhi-shu/">
        </link>
        <updated>2019-11-21T13:26:21.000Z</updated>
        <summary type="html"><![CDATA[<blockquote>
<p>树，是一种递归的数据机构，具有一种分层的逻辑结构，适用于表示具有层次关系结构的数据；</p>
</blockquote>
]]></summary>
        <content type="html"><![CDATA[<blockquote>
<p>树，是一种递归的数据机构，具有一种分层的逻辑结构，适用于表示具有层次关系结构的数据；</p>
</blockquote>
<!-- more -->
<h3 id="1树的定义">1.树的定义</h3>
<p>树是具有n的节点的有限集合，当n=0时，表示空树。任何树都应该具有：</p>
<ol>
<li>当树非空时，一棵树有且仅有一个根节点</li>
<li>除根结点外任一元素只有一个前驱节点</li>
<li>所有节点都可以具有零个或多个后继节点</li>
</ol>
<h3 id="2二叉树">2.二叉树</h3>
<p>定义：每个节点仅有两个后继节点，并且后继节点有左右之分，是有次序之分。</p>
<h4 id="1二叉树的链式存储结构">1.二叉树的链式存储结构</h4>
<pre><code class="language-c++">    typedef int ElemType;
    typedef struct BitNode *BitTree;
    struct BitNode
    {
        ElemType data;
        BTree lchild, rchild;
        /* data */
    };
    typedef BitTree BTree;

</code></pre>
<p>ps:在二叉树的链式表示中，若存在n个节点，则具有n+1个空链域。</p>
<h4 id="2二叉树的遍历">2.二叉树的遍历</h4>
<p>二叉树的遍历分为先序遍历、中序遍历、后序遍历以及层次遍历。<br>
* 先序遍历：先访问树的根结点再先序遍历该节点的的左子树最后先序遍历该节点的右子树；即顺序为根-&gt;左-&gt;右<br>
* 中序遍历：先中序遍历根节点的的左子树再访问树的根结点最后中序序遍历该节点的右子树；即顺序为左-&gt;根-&gt;右<br>
* 后序遍历：先后序遍历根节点的的左子树再后序序遍历该节点的右子树最后访问树的根结点；即顺序为左-&gt;右-&gt;根</p>
<pre><code class="language-c++">    void PreOrder(BTree &amp;T)
        {
            if (T != NULL)
            {
                // Visit(T);  先序遍历
                PreOrder(T-&gt;lchild);
                // Visit(T);  中序遍历
                PreOrder(T-&gt;rchild);
                // Visit(T);  后序遍历
            }
        }
</code></pre>
<p>以上为使用递归的方式完成的先、中、后序的遍历，除了使用递归的方式外还可以使用循环的方式来实现遍历，此时就需要借助栈来实现</p>
<pre><code class="language-c++">void InOrder(BTree T)
{
    Stack s;
    InitStack(s); // 初始化栈
    BTree t = T;
    while (t || !StackEmpty(s)) // 节点存在或者栈不为空执行循环
    {
        if (t)  // 若节点存在，将节点压入栈中，之后遍历左子树
        {
            Push(s, t);  
            t = t-&gt;lchild;
        }
        else  // 若节点不存在，将节点从栈中弹出，访问，之后遍历右子树
        {
            Pop(s, t);
            Visit(t);
            t = t-&gt;rchild;
        }
        /* code */
    }
}
</code></pre>
<p>以上为中序遍历的非递归实现方式，其中是借助栈来实现的，而对于层次遍历，则需要借助队列来实现</p>
<pre><code class="language-c++">
    void LevelOrder(BTree &amp;T)
    {
        SqQueue q;
        InitQueue(q);
        BTree t = T;
        EnQueue(q, t);  // 先将根节点入队，
        while (!QueueEmpty(q))
        {
            DeQueue(q, t); // 将结点出队，于此同时将该节点的左右子树的根结点入队 直到队列为空
            Visit(t);
            if (t-&gt;lchild)
            {
                EnQueue(q, t-&gt;lchild);
            }
            if (t-&gt;rchild)
            {
                EnQueue(q, t-&gt;rchild);
            }
        }
    }
</code></pre>
<p>以上对于二叉树的层次遍历使用到了队列来实现。</p>
<h4 id="3线索二叉树">3.线索二叉树</h4>
<p>在上面说过二叉树的链式存储时n个节点会产生n+1个空链域。线索二叉树就是将这些闲置的空链域利用起来，约定：若节点左子树为空，那么将其左子树指针指向其前驱元素，如柚子树为空，将其指针指向其后继节点，这个过程叫做二叉树的线索化。</p>
<pre><code class="language-c++">    typedef int ElemType;
    typedef struct BitNode *BitTree;
    struct BitNode
    {
        ElemType data;
        BTree lchild, rchild;
        int ltag,rtag; //添加字段ltag，rtag。 0 表示有左右孩子，1表示指向前驱或后继
        /* data */
    };
    typedef BitTree BTree;

    // 中序遍历线索化
    void InOrder(BTree &amp;T,BTree &amp;pre){ // pre表示其前驱节点
        if(T!= NULL){
            if(T-&gt;lchild == NULL){ // 左子树为空，则指向前驱节点
                T-&gt;ltag = 1;
                T-&gt;lchild = pre;
            }
            if(pre != NULL &amp;&amp; pre-&gt;rchild == NULL){ // 前驱节点右子树为空，则指向当前节点
                pre-&gt;rtag = 1;
                pre-&gt;rchild = T;
            }
            pre = T;
            InOrder(T-&gt;lchild,pre);
            InOrder(T-&gt;rchild,pre);
        }
    }

    // 创建线索二叉树
    void CreateInThread(BTree &amp;T){
    BTree pre = NULL;  // 初始值设置为NULL
    if(T){
        InThread(T,pre);
        pre-&gt;rchild = NULL; // 初始值右节点指向空
        pre-&gt;rtag = 1;
    }
}

</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[从零开始学数据结构之队列（二）]]></title>
        <id>http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-zhi-dui-lie-er/</id>
        <link href="http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-zhi-dui-lie-er/">
        </link>
        <updated>2019-11-18T10:04:57.000Z</updated>
        <summary type="html"><![CDATA[<p>之前学习了队列的顺序存储结构的代码实现，以及循环队列的实现，队列还可以使用链式存储结构来实现，并且链式存储的队列不会出现“溢出”的现象。</p>
]]></summary>
        <content type="html"><![CDATA[<p>之前学习了队列的顺序存储结构的代码实现，以及循环队列的实现，队列还可以使用链式存储结构来实现，并且链式存储的队列不会出现“溢出”的现象。</p>
<!-- more -->
<ol>
<li>数据结构如下</li>
</ol>
<pre><code class="language-c++">        typedef int ElemType;

            typedef struct LinkNode *LNode;
            struct LinkNode
            {
                ElemType data;
                struct LinkNode *next;
            };

            typedef struct LinkQueue *LQueue;
            struct LinkQueue
            {
                LinkNode *front, *rear;
            };         

</code></pre>
<ol start="2">
<li>
<p>具体代码实现</p>
<ol>
<li>队列初始化</li>
</ol>
</li>
</ol>
<pre><code class="language-c++">        void InitQueue(LQueue &amp;q)
            {
                LNode L = (LNode)malloc(sizeof(LNode));
                L-&gt;next = NULL;
                q-&gt;front = q-&gt;rear = L;
            }
</code></pre>
<ol start="2">
<li>判断队列是否为空</li>
</ol>
<pre><code class="language-c++">        bool QueueEmpty(LQueue &amp;q)
            {
                if (q-&gt;front == q-&gt;rear)
                {
                    return true;
                }
                return false;
            }
</code></pre>
<ol start="3">
<li>入队</li>
</ol>
<pre><code class="language-c++">        bool EnQueue(LQueue &amp;q, ElemType e)
                {
                    LNode L = (LNode)malloc(sizeof(LNode));
                    L-&gt;data = e;
                    L-&gt;next = q-&gt;rear-&gt;next;
                    q-&gt;rear-&gt;next = L;
                    q-&gt;rear = L;
                    return true;
                }
</code></pre>
<ol start="4">
<li>出队</li>
</ol>
<pre><code class="language-c++">        bool DeQueue(LQueue &amp;q, ElemType &amp;e)
                {
                    if (q-&gt;front == q-&gt;rear)
                    {
                        return false;
                    }
                    LNode L = (LNode)malloc(sizeof(LNode));
                    L = q-&gt;front-&gt;next;
                    e = L-&gt;data;
                    q-&gt;front-&gt;next = L-&gt;next;
                    if (L == q-&gt;rear)
                    {
                        q-&gt;front = q-&gt;rear;
                    }
                    free(L);
                    return true;
                }
</code></pre>
<ol start="5">
<li>读取队头元素</li>
</ol>
<pre><code class="language-c++">            bool GetHead(LQueue &amp;q, ElemType &amp;e)
                {
                    bool flag = QueueEmpty(q);
                    if (flag)
                    {
                        return false;
                    }
                    e = q-&gt;front-&gt;data;
                    return true;
                }
</code></pre>
<p>队列的链式存储适用于数组元素变动比较大的情形下，而且不存在队列满而产生的数据溢出的情况。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[从零开始学数据结构之队列（一）]]></title>
        <id>http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-3dui-lie/</id>
        <link href="http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-3dui-lie/">
        </link>
        <updated>2019-11-17T09:16:04.000Z</updated>
        <summary type="html"><![CDATA[<p>队列也是一种操作受限制的线性表，它拥有队首与队尾，并规定只能从队首删除元素（出队），从队尾添加元素（入队），队列的特点就是先进先出(First In First Out)。</p>
]]></summary>
        <content type="html"><![CDATA[<p>队列也是一种操作受限制的线性表，它拥有队首与队尾，并规定只能从队首删除元素（出队），从队尾添加元素（入队），队列的特点就是先进先出(First In First Out)。</p>
<!-- more -->
<ol>
<li>首先是队列的顺序存储结构，其数据结构如下</li>
</ol>
<pre><code class="language-c++">        #define MaxSize 50
        typedef int ElemType;
        typedef struct
        {
            ElemType data[MaxSize];
            int front, // 队头 允许出队
                rear;  // 队尾 允许入队
        } SqQueue;
</code></pre>
<ol start="2">
<li>其次是队列的常用操作：</li>
</ol>
<blockquote>
<p>同栈的操作一样，先判断一下队列的一些临界状态：</p>
</blockquote>
<ul>
<li>队列为空：则可以通过判断队列的首尾指针来确定，即q.front == q.rear，此时不允许出队。</li>
<li>队列已满：则通过判断队尾指针等于初始分配的最大空间，即q.rear == MaxSize（真的可以吗？🤔），此时不允许入队。</li>
</ul>
<p>具体代码实现如下：</p>
<ol>
<li>队列初始化</li>
</ol>
<pre><code class="language-c++">            void InitQueue(SqQueue &amp;q)
                {
                    q.front = q.rear = 0;
                }
</code></pre>
<ol start="2">
<li>判断队列是否为空</li>
</ol>
<pre><code class="language-c++">            bool QueueEmpty(SqQueue &amp;q)
            {
                if (q.front == q.rear)
                {
                    return true;
                }
                return false;
            }
</code></pre>
<ol start="3">
<li>入队</li>
</ol>
<pre><code class="language-c++">            bool EnQueue(SqQueue &amp;q, ElemType e)
                {
                    if (q.rear == MaxSize)
                    {
                        return false;
                    }
                    q.data[q.rear++] = e;
                    return true;
                }
</code></pre>
<ol start="4">
<li>出队</li>
</ol>
<pre><code class="language-c++">            bool DeQueue(SqQueue &amp;q, ElemType &amp;e)
            {
                if (q.front == q.rear)
                {
                    return false;
                }
                e = q.data[q.front--];
                return true;
            }
</code></pre>
<ol start="5">
<li>读取队头元素</li>
</ol>
<pre><code class="language-c++">            bool GetHead(SqQueue &amp;q, ElemType &amp;e)
                {
                    bool flag = QueueEmpty(q);
                    if (flag)
                    {
                        return false;
                    }
                    e = q.data[q.front];
                    return true;
                }
</code></pre>
<p>以上看似已经完成了队列的顺序存储，但此时又一个严重的问题就是，当队尾指针q.rear == MaxSize时，再执行出队操作，出队若干元素之后，再执行入队操作时会出现“上溢出”，明明数组data还有闲置的空间，这是一种“假溢出”的现象。那么现在开始，把上面那个data数组想象成一个圆环，将数组的首尾相接形成一个封闭的回路，那么就得到一个循环队列。在循环队列中解决这个问题就方便很多了，有很多的方法🤗，比如说</p>
<ol>
<li>在队列中加入一个表示当前存储的元素长度的字段size，</li>
<li>或者加入一个记录当前操作的字段flag。当flag为1时表示最后一次执行的时出队操作，此时判断q.front == q.rear，则为队空状态；为0时表示最后一次执行的是入队操作，此时判断q.front == q.rear，则为队满的状态。</li>
<li>还有一种方法就是牺牲一个存储空间，当q.front == q.rear+1时，就表示队列已满，队列为空时则仍是q.front == q.rear；不过之前临界的判断规则需要修改一下😹：</li>
</ol>
<ul>
<li>队列已满：q.front = (q.rear+1)%MaxSize;</li>
<li>队列元素的个数：count = (q.rear-q.front+MaxSize)%MaxSize;</li>
</ul>
<p>第三种方法的入队、出队具体代码实现如下：</p>
<pre><code class="language-c++">    // 入队

    bool EnQueue(SqQueue &amp;q, ElemType e)
    {
        if ((q.rear+1)%MaxSize == q.front) // 此时表示队列已满
        {
            return false;
        }
        q.data[q.rear] = e;
        q.rear = (q.rear+1)%MaxSize;
        return false;
    }

    // 出队

    bool DeQueue(SqQueue &amp;q, ElemType &amp;e)
    {
        if (q.front == q.rear)
        {
            return false;
        }
        e = q.data[q.front];
        q.front  = (q.front+1)%MaxSize;
        return true;
    }
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[从零开始学数据结构之栈]]></title>
        <id>http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-2-zhan/</id>
        <link href="http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-2-zhan/">
        </link>
        <updated>2019-11-13T12:51:22.000Z</updated>
        <summary type="html"><![CDATA[<p>栈是一种只允许在一端进行插入和删除的线性表，栈也是一种线性表,栈的特点是后进先出(Last In First Out)。</p>
]]></summary>
        <content type="html"><![CDATA[<p>栈是一种只允许在一端进行插入和删除的线性表，栈也是一种线性表,栈的特点是后进先出(Last In First Out)。</p>
<!-- more -->
<h2 id="顺序栈">顺序栈</h2>
<h3 id="首先来确定栈的数据结构">首先来确定栈的数据结构</h3>
<pre><code class="language-c++">    #define MaxSize 50
    typedef int ElemType;
    struct SqStack{
        ElemType data[MaxSize];  
        int top; // 栈顶指针，初始化时会设置为-1.
    } ;
</code></pre>
<p>然后来确定栈的一些特殊临届状态值：</p>
<ol>
<li>栈空状态：约定当栈顶指针s.top == -1 时，表示此时为空栈，不允许再进行出栈操作。</li>
<li>栈满状态：当栈顶指针s.top == MaxSize-1 时，表示此时栈已满，不允许再进行入栈操作。</li>
</ol>
<p>然后就可以来进行实现常用的对于栈的操作</p>
<h4 id="初始化空栈">初始化空栈</h4>
<pre><code class="language-c++">    void InitStack(SqStack &amp;s){
        s.top = -1;
    }
</code></pre>
<h4 id="判断是否为空栈">判断是否为空栈</h4>
<pre><code class="language-c++">    bool StackEmpty(SqStack &amp;s){
        if(s.top == -1){
            return true;
        }else{
            return false;
        }
    }
</code></pre>
<h4 id="入栈操作">入栈操作</h4>
<blockquote>
<p>入栈时将栈顶指针s.top+1,再将新元素入栈。</p>
</blockquote>
<pre><code class="language-c++">    bool Push(SqStack &amp;s,ElemType x){
        if(s.top == MaxSize-1){
            return false;
        }
        s.data[++s.top]=x;
        return true;
    }
</code></pre>
<h4 id="出栈操作">出栈操作</h4>
<blockquote>
<p>出栈时将元素出栈后再将栈顶指针减去一。</p>
</blockquote>
<pre><code class="language-c++">    bool Pop(SqStack &amp;s,ElemType &amp;x){
        if(s.top == -1){
            return false;
        }
        x = s.data[s.top--];
        return true;
    }
</code></pre>
<h4 id="获取栈顶元素">获取栈顶元素</h4>
<pre><code class="language-c++">    bool GetTop(SqStack &amp;s,ElemType &amp;x){
        if(s.top == -1){
            return false;
        }
        x = s.data[s.top]
        return true;
    }
</code></pre>
<p>除此之外还有共享栈，利用栈底不变的特性，将两个栈放在同一个一维数据空间内部以达到共享空间的目的。还有栈的链式存储结构与线性表的链式存储结构类似，只是具体实现方面有所不同。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[从零开始学数据结构之线性表]]></title>
        <id>http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-1-xian-xing-biao/</id>
        <link href="http://www.guotingyan.cn/post/cong-ling-kai-shi-xue-shu-ju-jie-gou-1-xian-xing-biao/">
        </link>
        <updated>2019-11-10T12:50:01.000Z</updated>
        <summary type="html"><![CDATA[<p>线性表是一种线性存储结构的，除了首位元素，每一个元素都有一个直接前驱；除了末尾元素，每一个元素都有一个直接后继。</p>
]]></summary>
        <content type="html"><![CDATA[<p>线性表是一种线性存储结构的，除了首位元素，每一个元素都有一个直接前驱；除了末尾元素，每一个元素都有一个直接后继。</p>
<!-- more -->
<pre><code> // 对于线性表的常用操作如下
	 InitList(&amp;L)  初始化线性表
	 Lenght(&amp;L) 返回表长
	 LocateElem(&amp;L,e) 按值来查找元素，在表L中查找指定元素
	 GetElem(&amp;L,i) 按序号查找元素，在表L查找指定位置的元素
	 ListInsert(&amp;L,i,e) 将元素e插如表L的指定位置i处
	 ListDelete(&amp;L,i,&amp;e) 将表L的第i个元素删除并用e返回删除元素的值
	 PrintfList(&amp;L) 打印线性表L
	 Empty(&amp;L) 判断线性表L是否为空，空则返回true，否则返回false
	 DestoryList(&amp;L) 销毁线性表L
</code></pre>
<h1 id="1顺序表">1.顺序表</h1>
<blockquote>
<p>顺序表是一种顺序存储结构的线性表，顺序表的逻辑相邻的两个元素在存储在物理结构上也相邻。</p>
</blockquote>
<h3 id="1-顺序表的数据结构">1. 顺序表的数据结构</h3>
<pre><code class="language-c++">		#define MaxSize 50
		typedef struct sq *SqList;
		struct sq {
				ElemType data[MaxSize];
				int Length;
		};
		typedef SqList SL;
</code></pre>
<h3 id="2-顺序表的常用操作">2.    顺序表的常用操作</h3>
<h4 id="按值查找">按值查找</h4>
<pre><code class="language-c++">
	int LocateElem(SL &amp;L, ElemType e){
			if(!L-&gt;Length){
					return 0;
			}
			for(int i = 0; i&lt; L-&gt;Length; i++){
					if(L-&gt;data[i] == e){
							return i+1;  // 线性表的元素是从下标为1开始计算，而数组是从下标0
							//开始计算，此处返回元素下标时应该加上1才能对应元素在线性表中的
							//下标
					}
			}
			return 0;
	}
</code></pre>
<h4 id="按序号查找">按序号查找</h4>
<pre><code class="language-c++">	
	ElemType GetElem(SL &amp;L,int i, ElemType e){
			if(i == 0){
					e = L-&gt;data[0];
					return e;
			}
			if(!L-&gt;Length || i&lt;1 || i &gt; L-&gt;Length){
					return e;
			}
			e = L-&gt;data[i-1];
			return e;
	}
</code></pre>
<h4 id="插入操作">插入操作</h4>
<pre><code>		int ListInsert(SL &amp;L, int i, ElemType e)
		{
				if (L-&gt;Length == MaxSize)  // 线性表已满 不允许插入操作
				{
						return 0;
				}
				if (i &lt; 1 || i &gt; L-&gt;Length+1)  // 超出线性表当前范围 不允许插入操作
				{
						return 0;
				}
				for (int j = L-&gt;Length; j &gt;= i; j--)
				{
						L-&gt;data[j] = L-&gt;data[j - 1];
				}
				L-&gt;data[i - 1] = e;
				L-&gt;Length++;
				return 1;
		}
</code></pre>
<h4 id="删除操作">删除操作</h4>
<pre><code>int ListDelete(SL &amp;L, int i, ElemType &amp;e)
{
    if (L-&gt;Length == 0 || i &gt; L-&gt;Length + 1)
    {
        return 0;
    }
    e = L-&gt;data[i - 1];
    for (int j = i - 1; j &lt; L-&gt;Length; j++)
    {
        L-&gt;data[j] = L-&gt;data[j + 1];
    }
    L-&gt;Length--;
    return 1;
}
</code></pre>
<h1 id="2链表">2.链表</h1>
<blockquote>
<p>由于顺序表的插入和删除操作需要移动大量的元素，影响运行效率，线性表的链式结构存储时不需要逻辑上相邻的两个元素在物理位置上也相邻。</p>
</blockquote>
<h3 id="1单链表的数据结构">1.单链表的数据结构</h3>
<pre><code class="language-c++">	typedef struct LNode *LinkList;
	struct LNode {
			ElemType data;
			LNode *next;
	};
	typedef LinkList List;
</code></pre>
<ol start="2">
<li>单链表的常用操作</li>
</ol>
<h4 id="创建单链表">创建单链表</h4>
<ol>
<li>头插法创建</li>
</ol>
<pre><code>// 头插法
List HeadInsert(List &amp;L)
{
    List p, s; // p为头指针
    int n = 20, i = 1;
    p = (List)malloc(sizeof(struct LNode));
    p = L;
    p-&gt;next = NULL; // 初始为空表
    // scanf(&quot;%d&quot;,&amp;n);
    while (i &lt; n)
    {
        s = (List)malloc(sizeof(struct LNode));
        s-&gt;data = i;
        s-&gt;next = p-&gt;next;
        p-&gt;next = s;
        i++;
    }
    return p;
}
</code></pre>
<ol start="2">
<li>尾插法</li>
</ol>
<pre><code>//  尾插法
List TailInsert(List &amp;L)
{
    List p, s; // 此处p为尾指针
    int n = 10, i = 1;
    p = (List)malloc(sizeof(struct LNode));
    p = L;
    // scanf(&quot;%d&quot;,&amp;n);
    while (i &lt; n)
    {
        s = (List)malloc(sizeof(struct LNode));
        s-&gt;data = i;
        p-&gt;next = s;
        p = s;
        i++;
    }
    p-&gt;next = NULL; // 尾节点指针置空
    return L;
}
</code></pre>
<h4 id="按值查找-2">按值查找</h4>
<pre><code class="language-c">
	List LocateElem(List &amp;L, ElemType e){
			List p = (List)malloc(sizeof(struct LNode));
			p = L;
			while(p &amp;&amp; p-&gt;data != e){
					p = p-&gt;next;
			}
			return p;
	}

</code></pre>
<h4 id="按序号查找-2">按序号查找</h4>
<blockquote>
<p>返回目标节点指针</p>
</blockquote>
<pre><code>List GetElem(List &amp;L, int i)
{
    LNode *p = (List)malloc(sizeof(struct LNode));
    p = L-&gt;next; // 头节点指针赋值给p
    int j = 1;
    if (i == 0)
    {
        return p;
    }
    if (i &lt; 1)
    {
        return NULL; // 若i 小于1且不为0，则i无效，返回NULL；
    }
    while (p &amp;&amp; j &lt; i)
    {
        j++;
        p = p-&gt;next;
    }
    return p;
}
</code></pre>
<h4 id="插入元素">插入元素</h4>
<blockquote>
<p>插入元素只需要修改指针指向即可</p>
</blockquote>
<pre><code>List ListInsert(List &amp;L, int i, ElemType e)
{
    List p, s;
    s = (List)malloc(sizeof(struct LNode));
    s-&gt;data = e;
    if (i == 0 || i == 1)
    {
        p = L;
    }
    else if (i &lt; 1)
    {
        return NULL;
    }
    else
    {
        p = GetElem(L, i - 1);
    }
    s-&gt;next = p-&gt;next;
    p-&gt;next = s;
    return s;
}
</code></pre>
<blockquote>
<p>tip:如果已知当前元素的指针，对于前插操作，链表需要遍历整个链表，此时的时间复杂度为O(n),如果换个思路，可以将元素插入目标位置之后，然后将前后两元素内部的数据进行互换同样可以完成目标✅，而此时的时间复杂度为O(1)</p>
</blockquote>
<h4 id="删除元素">删除元素</h4>
<pre><code>List ListDelete(List &amp;L, int i)
{
    List p, s;
    s = (List)malloc(sizeof(struct LNode));
    if (i == 0 || i == 1)
    {
        p = L;
    }
    else if (i &lt; 1)
    {
        return NULL;
    }
    else
    {
        p = GetElem(L, i - 1);
    }
    s = p-&gt;next;
    p-&gt;next = s-&gt;next;
    free(s);
}
</code></pre>
<p>线性表除了可以用顺序表、单链表之外还有更加方便各种操作的<strong>双链表</strong>、<strong>循环链表</strong>、<strong>循环单链表</strong>、<strong>循环双链表</strong>、<strong>静态链表</strong>等等。总之线性表是基础、超级重要啊啊啊！！！</p>
]]></content>
    </entry>
</feed>